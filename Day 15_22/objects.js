const arr= new  Array(1,'hi',6,true,null);
console.log(arr[4]);


const arr1 = new Array(5);     //if there is only one argument in it then its gonna suggest that this 5 is actually the length
console.log(arr);

const arr2 = [5];
console.log(arr2);         // [5] → creates an array with a single number

